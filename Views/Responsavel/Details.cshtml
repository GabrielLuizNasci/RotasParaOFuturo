@model RotasParaOFuturo.Models.Responsavel

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Details</h1>
<div class="container">
    <div class="row g-4">
        <!-- Nome -->
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body">
                    <h5 class="card-title text-primary"><i class="bi bi-person-circle"></i> Nome</h5>
                    <p class="card-text">@Html.DisplayFor(model => model.nome)</p>
                </div>
            </div>
        </div>

        <!-- Aniversário -->
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body">
                    <h5 class="card-title text-primary"><i class="bi bi-calendar-date"></i> Aniversário</h5>
                    <p class="card-text">@Html.DisplayFor(model => model.aniversario)</p>
                </div>
            </div>
        </div>

        <!-- CPF -->
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body">
                    <h5 class="card-title text-primary"><i class="bi bi-card-id-card"></i> CPF</h5>
                    <p class="card-text">@Html.DisplayFor(model => model.cpf)</p>
                </div>
            </div>
        </div>

        <!-- Continue com os demais campos... -->
    </div>

    <!-- Botões -->
    <div class="mt-4 text-end">
        <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-success me-2">
            <i class="bi bi-pencil-square"></i> Editar
        </a>
        <a asp-action="Index" class="btn btn-danger">
            <i class="bi bi-arrow-left"></i> Voltar à Lista
        </a>
    </div>
</div>
